<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- API 29+: system dark mode me WebView ka algorithmic force-dark dashboard gradients ko "white" dikha sakta hai; ribbon/tablet jaisa rake -->
        <item name="android:forceDarkAllowed">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Splash window bhi force-dark se safe — WebView load se pehle orange/peach tint kabhi mismatch -->
        <item name="android:forceDarkAllowed">false</item>
    </style>

    <!-- Native wedge rotate fullscreen — not Capacitor / MainActivity theme -->
    <style name="WedgeExpandTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@color/wedge_bg</item>
        <item name="android:statusBarColor">@color/wedge_bg</item>
        <item name="android:navigationBarColor">@color/wedge_bg</item>
    </style>
</resources>