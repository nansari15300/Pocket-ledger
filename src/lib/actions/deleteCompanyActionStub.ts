/**
 * Webpack static-build alias is `@/lib/actions/deleteCompanyAction` → yahi file.
 * Ab stub nahi — Firestore client par hi delete/restore (APK / static same as web client).
 */
export { deleteCompanyComplete, restoreCompany } from "../deleteCompanyFirestoreClient";
