# PL Server mode (`ledgerModes/plServer`)

**Adapters only.** Import frozen modules — do not duplicate or edit:
- `src/lib/plServer*.ts`
- `electron/*plServer*`
- PL Server settings components (AGENTS.md freeze list)

See `docs/LEDGER_MODE_ARCHITECTURE.md`.
