/**
 * Browser bundle: optional Node `canvas` / `@napi-rs/canvas` pdf.js shim — Turbopack alias target.
 * Webpack me `resolve.alias.canvas = false`; Turbopack ko string path chahiye.
 */
export {};

